# Schema: ../../AGENTS.md#repository-invariants.

[plugin]
name = "mcp"
language = "python"
coordinate = "temporalio-mcp"
registry = "pypi"
root-api = "temporalio.contrib.mcp"
maturity = "experimental"
status = "active"

[release]
allow-final = true

[ci]
runtime-versions = ["3.10", "3.14"]

[smoke]
imports = ["temporalio.contrib.mcp"]
